{
  "name": "@recall/extension-chrome",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && cp manifest.json dist/ && mkdir -p dist/popup && cp src/popup/popup.html dist/popup/ && cp src/popup/popup.css dist/popup/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.282",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
